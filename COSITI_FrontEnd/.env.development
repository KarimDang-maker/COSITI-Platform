# Chargé automatiquement par Vite en mode développement (`npm run dev`).
# Public dans le bundle (tout ce qui est préfixé VITE_ l'est) : uniquement l'URL de l'API, jamais un secret.
VITE_API_BASE_URL=http://localhost:8082/api/v1
